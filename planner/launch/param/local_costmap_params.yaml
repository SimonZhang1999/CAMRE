local_costmap:

  global_frame: odom
  robot_base_frame: base_link
  update_frequency: 5.0
  publish_frequency: 5.0
  static_map: false
  rolling_window: true
  width: 12.5
  height: 12.5
  resolution: 0.01
  # plugins:
  #   - {name: static_layer,            type: "costmap_2d::StaticLayer"} # http://wiki.ros.org/costmap_2d/hydro/staticmap because no map_server, so #
  #   - {name: obstacle_layer,          type: "costmap_2d::ObstacleLayer"}
  #   - {name: inflation_layer,         type: "costmap_2d::InflationLayer"}
